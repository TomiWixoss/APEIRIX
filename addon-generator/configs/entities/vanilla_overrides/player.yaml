# Vanilla Override: Player Entity
# Override player entity với custom attributes
# Note: Đây là entity override, không phải item, nên không có lore/texture

id: minecraft:player

# Attributes để modify player behavior
attributes:
  - id: empty_hand_combat
    config:
      context: combat
      damage: 0
